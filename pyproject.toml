
[project]
name = "deploy_thingy"
version = "0.1.0"
description = "Small deployment helper"
readme = "README.md"
dependencies = [
	"requests>=2.28",
    "pydantic>=2.13.3",
    "typer>=0.27.1"
]

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
